export default class GameObject {
    constructor(context, dx, dy, dWidth, dHeight) {
        this.context = context
        this.dx = dx
        this.dy = dy
        this.dWidth = dWidth
        this.dHeight = dHeight
    }

}